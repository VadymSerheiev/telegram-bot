const fetch = require("node-fetch");

// solution for heroku to always awake bot, send requests every 20 min
// setInterval(() => {
//   fetch("https://telegram-bot-pig.herokuapp.com/");
//   console.log("request sended");
// }, 1200000);
